import React from 'react';
import ReactDOM from 'react-dom/client';
import SearchImages from './components/App/App';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SearchImages />
  </React.StrictMode>
);
